# 案件管理Webアプリ 概要設計書

## 1. 目的

本Webアプリは、大学教員として日々発生する研究・教育・事務・委員会・出張・生活上の大型作業を、**案件（Case）**単位で管理し、連絡漏れ・対応漏れ・期限超過・作業の滞留を防ぐことを目的とする。

最大の目標は以下である。

> 案件を遅滞なく、連絡漏れなく、スムーズに完了させる手助けをすること。

本アプリは、単なるメールソフト、タスク管理ツール、カレンダーアプリ、ファイル管理システムではなく、これらを**案件中心に統合する個人用業務支援システム**として位置づける。

---

## 2. 基本方針

### 2.1 案件中心設計

本アプリの中心概念は **案件 / Case** である。

案件とは、以下のように定義する。

> 完了状態が存在している一連の仕事。

単発の作業ではなく、複数のメール、タスク、予定、ファイル、人物、期限、判断が関係するまとまりを案件として扱う。

### 2.2 外部サービス尊重

本アプリは、Gmail や Google Calendar を置き換えるものではない。

メールや予定の一次情報は、可能な限り既存の外部サービス側に残す。  
本アプリは、それらを案件単位で整理し、処理状態・タスク・文脈・ファイル・監査ログを統合する補助レイヤーとして機能する。

万が一、本Webアプリが停止した場合でも、Gmail や Google Calendar を確認すれば最低限の業務継続ができることを重視する。

### 2.3 個人用システム

本アプリは、当面は利用者本人のみが使う個人用システムとする。  
秘書や共同研究者など、他者にアカウントを発行して利用させることは想定しない。

---

## 3. 管理対象となる案件の例

本アプリで管理する代表的な案件は以下である。

- 自身の研究本体
- 学生の研究本体
- 倫理審査対応
- 授業準備
- 委員会業務
- 出張・学会参加に関する手続き
- 業績報告・科研費報告などの年次作業
- サーバー管理
- 生体信号の標準データベース管理
- 引っ越し
- 本アプリの開発
- CCSの教員としての事務タスク群

「論文執筆」「査読対応」「改稿」「共同研究者との調整」などは、単独では案件ではなく、原則として「自身の研究本体」などの案件に属するタスクとして扱う。

---

## 4. 主要概念

## 4.1 案件 / Case

案件は、本アプリの最上位管理単位である。

案件はフラットに管理する。  
親子関係は持たせず、研究・授業・事務・生活・開発などの分類はタグで表現する。

### 案件が持つ主な情報

- 案件名
- 説明
- タグ
- 進行状態
- ボール所在
- 期限情報
- 関連人物
- 関連メール
- 関連タスク
- 関連予定
- 関連ファイル
- 外部リンク
- メモ
- Case Context
- イベント履歴

### 案件の状態管理

案件の状態は単一ステータスではなく、複数軸で管理する。

#### 進行状態

- 未着手
- 進行中
- 完了

#### ボール所在

- 自分側
- 相手側
- 日付待ち
- 停滞
- なし

#### 期限状態

- 期限なし
- 期限あり
- 期限接近
- 期限超過

特に重要なのは、すべての案件が「自分側で止まっている」状態にならないように可視化することである。

---

## 4.2 タスク / Task

タスクは、ユーザー本人が実行する具体的なアクションである。

タスクは必ず1つの案件に属する。

### タスクに該当するもの

- メールに返信する
- リマインドを送る
- 書類を提出する
- 資料を確認する
- 授業スライドを作る
- 倫理審査書類を修正する
- サーバー更新を実施する
- 出張申請を行う

### タスクに該当しないもの

- 返事を待つ
- 相手の確認待ち
- 会議当日を待つ
- 審査結果を待つ

「待つ」こと自体はタスクではない。  
一定期間返信がない場合に「リマインドを送る」という能動的なタスクが発生する。

### タスクが持つ主な情報

- タスク名
- 所属案件
- 状態
- 締切
- 関連メール
- 関連人物
- 関連予定
- メモ
- 作成元
- 総作業時間見積もり
- 予定済み時間
- 作業済み時間

### タスクの状態

- 未着手
- 進行中
- 完了

作業時間の消化とタスク完了は別管理とする。  
見積もり時間を消化しても自動完了にはせず、完了はユーザーが明示的に行う。

---

## 4.3 Inbox / なんでも箱

すべてのタスクは原則として案件に属する。

ただし、どの案件に紐づけるべきか判断できないものや、単発だが一時的に保持したいもののために、特殊な案件として **Inbox / なんでも箱** を用意する。

これは「案件なしタスク」を許すのではなく、「未分類のものを入れる案件」として扱う。

---

## 4.4 人物 / Contact

人物管理は、単なるメールアドレス帳ではなく、案件推定やメール処理を補助する人物DBとして扱う。

### 人物が持つ主な情報

- 表示名
- メールアドレス
- タグ
- メモ
- 関連案件

所属や役職は専用カラムとして厳密に管理するのではなく、当面はタグで表現する。

### タグ例

- 筑波大
- CCS
- IIIS
- 事務
- 秘書
- 学生
- 研究員
- 共同研究者
- 企業
- S'UIMIN
- 学会

人物と案件の紐づきは、メールの関連案件自動推定に利用する。

---

## 5. メール機能

## 5.1 基本方針

メールは本アプリの主要な入口である。

アプリの初期画面は、既存の「まとめーる」に近いメール一覧を中心とする。  
ただし、単なるメール一覧ではなく、案件・タスク・予定・人物へ遷移できる入口として機能させる。

メールは必ず案件に紐づく必要はない。  
広告、SPAM、重要度の低い通知などは案件外のまま処理してよい。

一方で、仕事・研究・教育・事務・生活上の案件に関連するメールは、案件に紐づけて管理する。

## 5.2 まとめーる由来機能の継承

本アプリは「まとめーる」を単純に廃止するのではなく、案件管理アプリへ発展的に統合する。

すなわち、最終的な中心概念は案件 / Case とするが、既存の「まとめーる」で便利だったメール処理UI・メール処理機能は、可能な限り本アプリへ取り込む。

### 継承したい主な機能

- Gmailから直近メール・送信済みメールを取得する
- メールを日付別・重要度順・受信時刻順で一覧表示する
- High / Medium など重要度の高い未処理メールを集約表示する
- スレッド単位で本文、要約、返信草案、添付情報、会議候補を確認できる
- LLMによるメール要約、重要度判定、返信要否判定、推奨アクション生成を行う
- LLMによる返信候補・返信草案を生成する
- メール本文から締切候補・会議候補・予定候補を抽出する
- Gmail APIで返信または新規メールを送信できる
- Google Calendar APIで会議候補を登録・解除できる
- メールを処理済みにできる
- メール検索を行える
- Gmailで開くリンクを保持する
- 添付ファイル情報を表示する
- 画像添付をプレビューできる
- HTMLレポート的に、日付別のメール確認ビューを生成・表示できる

### ルール・フィルタ機能

既存の「まとめーる」にあったメールフィルタ・ルール登録機能は、本アプリでも維持する。

主な用途は以下である。

- 特定送信者のメールをスキップする
- 特定件名・語句を含むメールをスキップする
- 特定条件のメールを必ず解析対象にする
- LLMを使わずに事前分類する
- 固定の重要度、カテゴリ、要約、推奨アクションを付与する
- 広告、通知、ニュースレター等を案件管理のノイズから外す

フィルタ条件としては、少なくとも以下を扱えるようにする。

- From
- To
- Cc
- 件名
- 本文に含む語句
- 本文に含まない語句
- 添付有無
- 添付ファイル名
- Gmailラベルまたはカテゴリ

### 既存メールからの作成機能

既存の受信メール・送信済みメールを起点として、以下を容易に作成できるようにする。

- 新規案件
- タスク
- 予定
- 返信草案
- アドレス帳上の人物
- メールフィルタ
- LLM追加指示

特に、メール詳細画面から「この送信者に関するフィルタを作る」「この件名パターンをスキップする」「このメールから案件を作る」「このメールからタスクを作る」といった操作をワンボタンまたは少ない操作で行えることを重視する。

### アドレス帳・宛先補完機能

「まとめーる」に存在したアドレス帳機能も継承する。

- 送信者をアドレス帳に登録する
- 送信済みメールの宛先から連絡先を登録する
- 名前、組織、タグ、メールアドレスで宛先補完する
- タグを宛先指定に使う
- `KDE&秘書` のようなタグAND合成で送信先を絞り込む
- タグチップにより、展開される送信先を確認する

本アプリでは、これらを案件・人物タグ・関連案件推定と統合する。

### LLM追加指示・生成指示

メール解析や返信生成に対して、ユーザーが追加指示を登録できる機能を維持する。

例：

- 査読依頼メールでは専門分野との合致度を表示する
- 学生からのメールでは次に行うべき作業を明示する
- 事務メールでは締切と提出先を強調する
- 返信文面は簡潔にし、今後の対応を中心に書く

これらの指示は、案件タグ、人物タグ、送信者、件名、本文キーワードなどに応じて適用できるようにする。

### 位置づけ

まとめーる由来の機能は、単なる互換機能ではなく、本アプリにおけるメール入口の中核機能として扱う。

ただし、最終的な整理単位はメールではなく案件である。
メール処理機能は、案件・タスク・予定・人物・ファイルへ流し込むための入口として再設計する。

## 5.3 メールと案件の紐づけ

関連案件の紐づけは、最初にLLMが自動推定する。

ユーザーは、推定が間違っていた場合だけ手動で修正する。  
関連案件を毎回手動で選ぶ運用は避ける。

### 関連案件推定に利用する情報

- 送信者
- 宛先
- CC
- 件名
- 本文
- 添付ファイル名
- 送信者の人物タグ
- 送信者に関連する案件
- 過去に同じ案件へ紐づけられたメール
- 案件のCase Context

## 5.4 メールからの操作

各メールから以下の操作を行える。

- 返信する
- タスク化する
- 予定化する
- 関連案件を変更する
- このメールから新規案件を作成する
- 処理済みにする
- 不要として処理済みにする

## 5.5 メールの処理状態

メールの処理状態は、個々の受信メール単位で管理する。

状態は以下の2値とする。

- 未処理
- 処理済み

メールスレッド自体には独自の処理状態を持たせない。  
ただし、メールスレッド内のすべての受信メールが処理済みになっていることを目指す。

### 処理済みになる条件

以下のいずれかが実行された場合、そのメールは処理済みになる。

- 返信を送る
- タスク化する
- 予定化する
- 手動で処理済みにする
- 明示的に無視 / 不要として処理済みにする

以下の操作だけでは処理済みとはしない。

- 要約を読む
- 返信草案を作る
- 案件に紐づける
- 新規案件を作成する
- 関連人物を確認する

情報整理と処理完了は区別する。

## 5.6 Gmailスター連携

Gmail上でスターが付いているメールは、アプリ上で重要度 High として扱う。

また、LLMが重要度 High と判断したメールには、Gmail側にもスターを付与する。

ただし、Gmailの既読・未読状態は変更しない。

### 重要度Highの根拠

- Gmail側でスターが付いていた
- LLMが重要と判定した
- ユーザーが手動で重要と判断した

---

## 6. カレンダー機能

## 6.1 基本方針

予定の正本は Google Calendar とする。

本アプリは、予定そのものを独自管理するのではなく、Google Calendar上の予定と案件・タスクを紐づける。

### アプリ側で保持する情報

- Google Calendarの予定ID
- 関連案件
- 関連タスク
- 元メール
- 補助メモ

Google Calendarの説明欄には、関連案件や元メールなど、人間が読んでも意味が分かる情報を記録する。

## 6.2 基本機能

- 今日・今週の予定表示
- メールから予定候補を抽出
- ワンボタンでGoogle Calendarへ予定作成
- 予定と案件の紐づけ
- 案件詳細画面で関連予定を表示

## 6.3 日程調整支援

メールで日程調整が必要になった場合、Google Calendarを参照して空いている時間を提示する。

### 主な機能

- メール本文から条件を抽出
- 所要時間を推定または指定
- 空き時間候補を提示
- 候補日程を返信案に組み込む

## 6.4 タスクのカレンダー配置

タスクは、必要に応じてGoogle Calendar上に作業ブロックとして配置できる。

### 操作イメージ

1. タスク上で「カレンダーに配置」を選択
2. 今回確保したい作業時間を指定
3. 直近1週間または締切前までの空き時間を検索
4. 候補から1つを選択
5. Google Calendarに作業ブロックとして登録

タスクには総作業時間見積もりを持たせるが、カレンダーに何時間分配置するかはその都度決める。

### 時間管理

タスクは以下の時間情報を持つ。

- 総見積もり時間
- 予定済み時間
- 作業済み時間
- 残作業時間

## 6.5 予定作成時の準備タスク生成

メールから予定を作成する際、必要に応じてLLMが事前準備タスク候補を生成する。

例：

- 会議資料を確認する
- 共有用スライドを作成する
- Zoom URLを確認する
- 出張経路を確認する

---

## 7. ファイル・ストレージ機能

## 7.1 基本方針

ファイルの正本は、アプリ内ストレージに置く。

クラウドストレージを正本にはしない。  
倫理審査、学生情報、共同研究、未公開論文、契約関係、医療・生体信号関連資料を扱う可能性があるため、ファイルはローカルな管理を基本とする。

## 7.2 ファイルの種類

ファイルにはOriginを持たせる。

### Origin種別

- Mail
- Upload
- Generated
- External Link

### Mail

Gmail添付から自動保存されたファイル。

### Upload

ユーザーが手動で案件にアップロードしたファイル。

### Generated

アプリ内で生成されたファイル。

### External Link

GitHub、Overleaf、Google Drive、投稿システムなど、アプリ内に実体を持たない外部リンク。

## 7.3 ファイルカード表示

案件ページでは、1ファイルを1カードとして表示する。

カードには以下を表示する。

- ファイル名
- Origin
- 添付元メールまたはアップロード情報
- 機密度タグ
- 更新日時
- 操作ボタン

例：

- Origin: Mail
- Origin: Upload
- Origin: External Link

## 7.4 Gmail添付の保存

案件に紐づいたメールの添付ファイルは、自動的にアプリ内ストレージへ保存する。

添付ファイルには、添付元メールの情報を必ず保持する。

メールを別の案件に移動した場合、添付ファイルもそのメールの移動に合わせて、表示される案件が変わる。

つまり、メール添付由来のファイルは、添付元メールの案件紐づきに連動して見え方が変わる。

## 7.5 手動アップロードファイル

手動アップロードされたファイルは、指定された案件に直接所属する。

## 7.6 バージョン管理

ファイルのバージョン管理は、MVPでは独自管理とする。

同一ファイルの再アップロードや更新があった場合、過去バージョンを保持できるようにする。

GitHub的なバージョン管理の利用は将来拡張とする。  
ただし、研究案件や開発案件では、案件ページからGitHubリポジトリへの動線を持たせる。

---

## 8. 機密度管理

## 8.1 基本方針

ファイルには機密度タグを付与できる。

機密度は、単一分類ではなくタグ方式とする。

### 機密度タグ例

- 通常
- 学内限定
- 共同研究限定
- 個人情報あり
- 医療・生体信号関連
- 倫理審査関連
- 契約関連
- LLM利用禁止

## 8.2 LLM利用制御

ファイルの機密度は、LLMにファイル内容を読ませるかどうかの制御に利用する。

低機密と判断されたファイルは、自動で本文抽出・要約を行ってよい。

高機密、不明、危険そうなファイルは、自動ではLLMに読ませない。  
ユーザーが明示的に許可した場合のみ処理する。

## 8.3 初期判定

ファイル保存時には、ファイル名、メール件名、送信者、案件タグ、メール本文などから、システムが暫定的に機密度を判定する。

高機密かどうかの判定のために、最初からLLMへファイル本文を渡すことは避ける。

---

## 9. LLM支援機能

## 9.1 基本方針

LLMは、ユーザーの操作を代替するのではなく、案件・メール・タスク・予定・ファイルの整理と判断を補助する。

### 主な用途

- メール要約
- 関連案件推定
- メール重要度判定
- 返信草案生成
- メールからのタスク抽出
- メールからの予定抽出
- 日程調整返信案の作成
- 予定作成時の準備タスク候補生成
- 低機密ファイルの要約
- Case Context更新支援

## 9.2 Case Context

各案件は、LLM処理用の圧縮文脈である **Case Context** を持つ。

### Case Contextに含める情報

- 案件概要
- 現在の状況
- 関係者
- 重要な固有名詞
- 未解決事項
- 最近の重要イベント
- 返信方針
- 注意事項

案件に関連するメールを処理する際には、その案件のCase Contextを読み込んだ状態でLLM処理を行う。

## 9.3 イベントログと将来RAG化

MVPでは本格的なRAGは実装しない。

ただし、将来RAG化できるように、メール、タスク、予定、ファイル、メモなどの重要な出来事を案件イベントとして保存する。

### 案件イベント例

- メール受信
- メール処理済み
- 返信送信
- タスク作成
- タスク完了
- 予定作成
- ファイル保存
- ファイル要約
- 案件メモ更新
- Case Context更新

将来的には、イベントログ、メール要約、ファイル要約、案件メモなどを検索対象とし、関連情報をLLMに渡せるようにする。

---

## 10. 監査ログ・操作履歴

## 10.1 基本方針

監査ログは厚めに取る。

特に重視するのは、アプリの内部挙動の記録ではなく、**ユーザーがどの情報を認識・閲覧・取得した可能性があるか**を後から追跡できることである。

## 10.2 セッションログ

ログインセッションごとに以下を記録する。

- セッションID
- 証明書ID
- 端末名
- IPアドレス
- User-Agent
- ログイン時刻
- 最終アクセス時刻
- ログアウト時刻またはタイムアウト時刻

## 10.3 メール接触ログ

メールについては、以下を区別して記録する。

- メール一覧に表示された
- メールの存在を認識した可能性がある
- メール本文を開いた
- 返信した
- タスク化した
- 予定化した
- 処理済みにした
- 添付ファイルを表示した
- 添付ファイルをダウンロードした

「存在を認識した可能性がある」とは、メール一覧画面にそのメールが表示された状態を指す。

## 10.4 ファイル接触ログ

ファイルについては、以下を区別して記録する。

- ファイルカードが表示された
- ファイル詳細を開いた
- プレビューした
- ダウンロードした
- LLMに投入した

## 10.5 重要操作ログ

以下の操作は監査ログに記録する。

- Gmail送信
- Gmailスター付与
- Google Calendar予定作成・変更
- 機密ファイル閲覧
- 機密ファイルダウンロード
- 機密ファイルのLLM投入
- 機密度タグ変更
- 案件紐づけ変更
- ファイル削除
- 案件削除
- バックアップ実行
- 復元実行
- 証明書発行
- 証明書失効
- 設定変更

## 10.6 ログ容量方針

監査ログは厚めに取る。  
容量面では、ファイルストレージの方が監査ログよりはるかに大きくなる見込みであり、ログ容量を理由に記録を削る必要は低い。

古いログは必要に応じて圧縮・アーカイブする。

---

## 11. 認証・アクセス制御

## 11.1 基本方針

本アプリは本人専用とする。

認証は厳しくするが、日常操作ごとの追加認証や確認ダイアログは原則として設けない。  
使い勝手を損なわず、アクセス経路自体を強く制限する。

## 11.2 認証方式

- Tailscaleによるネットワーク到達性制限
- クライアント証明書認証

有効なTailscale経路からアクセスし、かつ有効なクライアント証明書を持つ端末のみが利用できる。

## 11.3 クライアント証明書

- 端末ごとに発行
- 有効期限は原則6か月
- 失効管理あり
- 証明書更新はタスク化する

## 11.4 証明書更新タスク

「本アプリの保守・運用」案件を用意し、その中に証明書更新タスクを自動生成する。

### 自動生成されるタスク例

- クライアント証明書を更新する
- 証明書の有効期限を確認する
- 不要な証明書を失効する

---

## 12. バックアップ・復旧

## 12.1 基本方針

ファイル正本をアプリ内ストレージに持つため、バックアップは重要機能として扱う。

### 正本

- Ubuntuサーバー内のアプリストレージ  
  ただし、Ubuntuサーバー購入前はWindows上の仮本番ストレージを利用可能とする。

### バックアップ先

- 外付けHDD/SSD

クラウドへのバックアップは原則としない。  
機密情報を含むため、バックアップは暗号化して保存する。

## 12.2 バックアップ頻度

- 週1回の増分バックアップ
- 四半期に1回のフルバックアップ

## 12.3 バックアップ対象

- アプリDB
- ファイルストレージ
- 設定情報
- 証明書管理情報
- 監査ログ
- Case Context
- イベントログ

## 12.4 世代管理

容量が許す限り、過去バックアップは削除しない。

外付けHDD/SSDが満タンに近づいた場合は、新しいメディアへ交換する。  
どのバックアップがどのメディアに保存されているかを記録する。

## 12.5 復旧方針

任意のバックアップ日時を選んで復元できるようにする。

バックアップは取得するだけでなく、復元可能であることを確認する必要がある。  
そのため、復旧テストも定期的にタスク化する。

---

## 13. 画面構成の概要

## 13.1 トップ画面

トップ画面は、メール入口を中心とする。

### 表示要素

- 今日の予定
- 重要な注意事項
- 未処理メール一覧
- メール要約
- 関連案件推定
- ワンボタン操作

### ワンボタン操作例

- 返信
- タスク化
- 予定化
- 新規案件作成
- 処理済み
- 不要として処理済み

## 13.2 主要画面

- メール一覧
- 案件一覧
- 案件詳細
- タスク一覧
- カレンダー連携画面
- 人物一覧
- ファイル一覧
- 監査ログ
- 設定
- 保守・運用

## 13.3 案件詳細画面

案件詳細画面では、以下を集約表示する。

- 案件概要
- 状態
- 関連メール
- 関連タスク
- 関連予定
- 関連人物
- 関連ファイル
- 外部リンク
- Case Context
- イベント履歴
- 監査ログへの導線

---

## 14. 環境の考え方

## 14.1 基本方針

開発・テスト環境と本番環境は分離する。

### Windows側

- 開発環境
- テスト環境
- VS Code + GitHubによる実装作業
- 本番データは原則保持しない

### Ubuntu側

- 本番環境
- 実際のGmail/Google Calendar連携
- 本番DB
- 本番ファイルストレージ
- 監査ログ
- 証明書認証
- 外付けHDD/SSDバックアップ

Ubuntuサーバー購入前は、Windows上で仮本番運用することを許容する。  
ただし、将来的にはUbuntuを本番環境とし、Windowsは開発・テスト環境へ移行する。

## 14.2 Production / Staging

本番環境とテスト環境では、以下を分離する。

- DB
- ファイルストレージ
- 監査ログ
- Gmail/Calendar連携設定
- LLM設定
- バックアップ先
- 認証設定

テスト環境では、Gmail送信や本番Google Calendarへの書き込みを原則行わない。

---

## 15. 運用保守案件

本アプリ自身の運用も、案件として管理する。

### 案件名

- 本アプリの保守・運用

### 自動生成タスク例

- クライアント証明書を更新する
- バックアップ結果を確認する
- 四半期フルバックアップを実行する
- 外付けHDD/SSDを交換する
- 復旧テストを実施する
- システム更新を適用する

---

## 16. MVPで重視する範囲

概要設計上、本アプリは大規模である。  
ただし、初期実装では以下を優先する。

## 16.1 初期実用ライン

- 案件管理
- メール一覧
- まとめーる由来のメール確認UI
- LLMによる返信草案生成
- メールフィルタ・ルール登録
- メールの未処理 / 処理済み管理
- LLMによる関連案件推定
- メールからの返信・タスク化・予定化
- 今日の予定表示
- Gmailスター連携
- 基本的な人物タグ管理
- アドレス帳・宛先補完
- 監査ログ
- 認証
- バックアップ

## 16.2 次段階

- タスクのカレンダー配置
- 日程調整支援
- 予定作成時の準備タスク生成
- ファイル保存
- 添付ファイル自動保存
- 機密度管理
- Case Context

## 16.3 将来拡張

- 案件内RAG
- 高度なファイル検索
- ローカルLLM利用
- GitHub / Overleaf等との高度連携
- より高度な運用ダッシュボード

---

## 17. まとめ

本アプリは、大学教員の業務を「案件」という単位で整理し、メール、タスク、予定、人物、ファイル、LLM文脈、監査ログを統合する個人用業務支援システムである。

特に重視する点は以下である。

- メールを入口とした案件管理
- まとめーる由来のメール処理UI・返信候補生成・フィルタ登録機能の継承
- 関連案件の自動推定
- 未処理メールの確実な消化
- 自分側で止まっている案件の可視化
- タスクとカレンダーの連動
- 予定作成時の準備タスク生成
- 機密ファイルの安全な管理
- Case ContextによるLLM文脈管理
- ユーザー行動を追跡できる厚めの監査ログ
- Tailscale + クライアント証明書による強い認証
- 外付けHDD/SSDへの暗号化バックアップ

本アプリは、GmailやGoogle Calendarを置き換えるものではなく、それらを利用しながら、案件遂行を支援する統合レイヤーとして設計する。
